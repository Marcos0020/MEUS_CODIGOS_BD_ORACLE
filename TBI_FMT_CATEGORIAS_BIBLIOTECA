CREATE OR REPLACE TRIGGER ADMIN.TBI_FMT_CATEGORIAS_BIBLIOTECA
BEFORE INSERT OR UPDATE ON BIBLIOTECA
FOR EACH ROW
BEGIN
  -- Formata a coluna CATEGORIAS substituindo vírgulas e pontos e vírgulas por barras
:NEW.CATEGORIAS := REGEXP_REPLACE(:NEW.CATEGORIAS, '[,;]', ' / ');

END;